# 基于RAG的本地学术知识库问答Agent：从检索到生成的全链路实践

> 作者：李国杰 | 独立开发（2025.3 - 2026.4）
> 系列：CV 算法工程笔记 · 第 04 篇
> 代码开源：https://github.com/Aeijou37/rag-academic-qa-agent

## TL;DR

- **任务**：本地学术文档问答——上传论文/技术报告，用自然语言提问，系统检索相关段落并生成带来源溯源的回答。全流程本地运行，不依赖外部 API。
- **挑战**：小模型（<7B）指令泄露与原文复述、学术文档格式复杂（公式/表格/图表）、检索质量瓶颈、多文档跨文档检索。
- **做法**：LangChain + Chroma 构建检索链路 → chat_template 标准化聊天格式 → 强约束 System Prompt + 后处理清洗 → MMR 检索 + 查询改写 + 来源溯源。
- **结果**：支持 PDF/DOCX/TXT/MD 多格式文档与多轮问答，掌握 RAG 架构设计、Prompt Engineering 与本地模型部署全链路能力。
- **方法学沉淀**：RAG 的瓶颈不在生成而在检索——检索不到的，生成再好也是编的（幻觉）。**检索质量 > 生成质量 > Prompt 工程**，和 CXMT 的"数据策略 > 模型增强 > 损失函数"同构。

---

## 一、问题定义：为什么本地学术问答不是"调个API"那么简单

### 1.1 任务背景

学术研究中有大量论文、技术报告、实验笔记，但检索和理解它们的效率很低——关键词搜索只能匹配字面，无法理解语义；通用的 ChatGPT 不了解你的私有文档；在线 API 有隐私和费用顾虑。

这个项目的目标是：**在本地搭建一个学术知识库问答系统**——上传文档后，用自然语言提问，系统自动检索相关段落，生成回答，并标注来源出处。

### 1.2 为什么这个任务有深度

| 难点 | 具体表现 | 为什么难 |
|---|---|---|
| 小模型能力有限 | 本地部署的模型通常 <7B，推理能力和指令遵循能力远弱于 GPT-4 | 容易出现指令泄露（把 System Prompt 内容输出给用户）和原文复述（直接复制原文大段内容而非概括） |
| 学术文档格式复杂 | 论文里有公式、表格、图表、引用、多栏排版 | 文本提取质量差，公式变成乱码，表格被打散，直接影响检索和生成 |
| 检索质量瓶颈 | 学术术语高度专业化，query 和文档的语义匹配不直接 | "这个方法的损失函数是什么"要检索到"LDAM"相关段落，但文中不一定出现"损失函数"这个词 |
| 多文档管理 | 用户可能上传多篇论文，需要跨文档对比和检索 | 不同文档的 chunk 不能混淆，来源溯源要准确到哪篇文档的哪一段 |
| 幻觉控制 | 本地小模型容易"编造"文档中没有的内容 | 学术问答场景下，编造的结论比"不知道"更有害 |

> **教训 #1**：RAG 的核心矛盾是"检索到了但生成不好" vs "检索不到所以只能编"。前者是生成模型的问题，后者是检索系统的问题。在实际开发中，后者才是主要瓶颈——**检索不到的，生成再好也是幻觉**。

---

## 二、基线：最朴素的 RAG 链路

### 2.1 基线架构

最朴素的 RAG 链路：

```
文档 → 文本提取 → 固定长度切分 → Embedding → Chroma 存储
                                                    ↓
用户提问 → Embedding → 相似度检索 Top-K → 拼接 Context → LLM 生成
```

### 2.2 基线配置

| 项 | 配置 |
|---|---|
| 文本提取 | PyPDF2 / python-docx |
| 切分策略 | 固定长度 500 字符，重叠 50 |
| Embedding 模型 | BAAI/bge-large-zh-v1.5（中文语义检索效果好，本地可跑） |
| 向量数据库 | Chroma（持久化存储） |
| 检索策略 | 纯相似度 Top-K（K=4） |
| 生成模型 | Qwen2.5-7B-Chat（本地部署，中文生成质量好） |
| Prompt | 简单拼接："根据以下内容回答问题：{context}\n\n问题：{question}" |

### 2.3 基线的三个致命问题

**问题1：小模型指令泄露**

基线 Prompt 直接拼接 context 和 question，小模型会把 System Prompt 的内容"泄露"到回答里——比如回答开头出现"你是一个学术问答助手，请根据以下内容..."。用户看到这种回答会觉得系统不专业。

**问题2：原文复述**

小模型不擅长"概括"，它倾向于直接复制检索到的原文大段内容作为回答。用户问"这篇论文的主要贡献是什么"，得到的回答是原文 Introduction 的前三段逐字复制，而不是一句话概括。

**问题3：检索质量差**

固定长度切分会把一个完整的段落/章节切成碎片。学术论文的"方法"部分可能跨多个 chunk，检索到的是碎片化的片段，上下文不完整，生成质量自然差。

> **教训 #2**：基线的三个问题分别对应 RAG 的三个环节——检索（切分策略）、拼接（Prompt 设计）、生成（小模型能力）。三个环节的问题不能用同一个方法解决，必须分别针对性优化。

---

## 三、方法：四条线索推进

### 线索 A：检索链路优化（检索质量是瓶颈）

**动机**：基线的固定长度切分破坏了文档的语义完整性。学术文档有天然的语义边界（章节、段落、标题），应该按语义切分而非字符数。

**优化点**：

| 优化 | 做法 | 效果 |
|---|---|---|
| 语义切分 | 用 LangChain 的 RecursiveCharacterTextSplitter，按段落/标题优先切分 | 保留段落完整性 |
| chunk 大小调优 | 最终用 chunk_size=800、overlap=150（中文按字符计），通过人工检查检索结果反复调优 | 平衡检索精度和上下文完整性 |
| MMR 检索 | Maximum Marginal Relevance，在相似度基础上加入多样性约束 | 避免检索到4个几乎一样的 chunk |
| 查询改写 | 用 LLM 把用户问题改写为更适合检索的表述 | "这个方法的损失函数"→"LDAM loss margin" |

**MMR 为什么有效**：

纯相似度检索的问题是：如果文档里有一个段落被重复了3次（如论文摘要和 Introduction 的重复部分），Top-4 检索可能返回3个几乎一样的 chunk，浪费了上下文窗口。MMR 在选择下一个 chunk 时，不仅看与 query 的相似度，还看与已选 chunk 的差异度——**既要相关，又要不重复**。

**查询改写为什么有效**：

用户问"这个方法怎么解决长尾问题的"，但论文原文可能写的是"LDAM margin 机制为少数类扩大决策边界"。直接用原文检索，query 和文档的词汇不匹配。查询改写让 LLM 先把问题转成更接近文档表述的形式，提高检索命中率。

> **教训 #3**：检索优化的核心思路是"让检索看到的和文档写的对齐"——切分策略让文档保留语义完整性，MMR 让检索结果不冗余，查询改写让 query 和文档语言对齐。三个优化分别作用于文档侧、检索侧和查询侧。

### 线索 B：chat_template 标准化（解决指令泄露）

**动机**：基线直接拼接文本送入模型，没有使用标准聊天格式。小模型在非标准格式下容易"混淆"哪些是系统指令、哪些是用户输入、哪些是模型应该输出的内容。

**做法**：使用模型的 chat_template 构建标准聊天格式：

```python
# 标准聊天格式（以 Qwen 为例）
messages = [
    {"role": "system", "content": system_prompt},
    {"role": "user", "content": f"参考信息：{retrieved_context}\n\n问题：{question}"},
]
text = tokenizer.apply_chat_template(messages, tokenize=False, add_generation_prompt=True)
```

**为什么有效**：chat_template 用特殊的 token（如 `<|im_start|>system` / `<|im_start|>user` / `<|im_start|>assistant`）明确分隔不同角色的内容。模型在训练时就是用这种格式训练的，用它推理时指令遵循能力更强，不会把 system prompt 的内容"泄露"到回答里。

> **教训 #4**：小模型不像 GPT-4 那样能从非结构化文本中"推断"指令——它需要**格式化 的输入**才能正确区分指令和内容。用 chat_template 不是"最佳实践"，而是小模型的**必需品**。

### 线索 C：强约束 System Prompt + 后处理（解决原文复述）

**动机**：即使用了 chat_template，小模型仍然倾向于复制原文而非概括。需要在 Prompt 层面强制约束行为，在后处理层面清洗不符合规范的输出。

**System Prompt 设计**：

```
你是一个学术文档问答助手。请严格遵守以下规则：

1. 回答必须基于提供的参考信息，不得编造文档中不存在的内容。
2. 用自己的语言概括，不要大段复制原文。引用原文时不超过50字。
3. 如果参考信息不足以回答问题，回答"根据已有文档无法回答该问题"。
4. 回答末尾标注来源：[来源：文档名，第X段]
5. 回答控制在200字以内，简洁准确。
```

**后处理清洗机制**：

```python
def post_process(response: str, retrieved_context: str) -> str:
    # 1. 去除指令泄露残留
    for keyword in ["你是一个", "请遵守", "system", "assistant"]:
        response = response.replace(keyword, "")
    
    # 2. 检测原文复述：如果回答中有超过50字的连续片段与原文完全一致
    for chunk in retrieved_context.split("\n"):
        if len(chunk) > 50 and chunk in response:
            response = response.replace(chunk, f"[原文引用：{chunk[:30]}...]")
    
    # 3. 格式校验：确保有来源标注
    if "来源" not in response:
        response += "\n[来源：未能准确定位]"
    
    return response
```

> **教训 #5**：小模型的"不可靠"需要双层防护——Prompt 层面约束行为（"不要复制原文"），后处理层面兜底（检测到复制就替换）。只靠 Prompt 不够，因为小模型的指令遵循能力有限；只靠后处理也不够，因为后处理是规则匹配，无法处理所有变体。两层配合才能把质量稳定在可用水平。

### 线索 D：多文档管理与来源溯源

**动机**：用户上传多篇论文后，需要跨文档检索和对比。比如"比较这两篇论文的方法差异"。

**实现**：

| 功能 | 做法 |
|---|---|
| 多文档管理 | 每篇文档独立存入 Chroma，用 metadata 标记文档名和来源 |
| 跨文档检索 | 检索时不限定文档，返回 Top-K 中可能来自不同文档的 chunk |
| 来源溯源 | 每个 chunk 的 metadata 包含文档名和段落位置，生成回答时标注 |
| 查询改写（多文档） | "比较这两篇论文" → 改写为两个独立检索："论文A的方法" + "论文B的方法" |

---

## 四、消融实验

由于这是一个工程项目而非研究实验，消融以"开关各功能模块"的方式验证：

| 配置 | 指令泄露 | 原文复述 | 检索命中率 | 回答质量 |
|---|:---:|:---:|:---:|:---:|
| 基线（无任何优化） | 严重 | 严重 | 低 | 差 |
| + chat_template | 消除 | 仍有 | 低 | 略改善 |
| + 强约束 Prompt | 消除 | 减轻 | 低 | 改善 |
| + 后处理清洗 | 消除 | 消除 | 低 | 改善 |
| + 语义切分 | 消除 | 消除 | **提升** | 改善 |
| + MMR 检索 | 消除 | 消除 | **提升** | 改善 |
| + 查询改写 | 消除 | 消除 | **显著提升** | **显著改善** |
| 全部叠加 | 消除 | 消除 | 高 | 好 |

> 注：上表"回答质量"为定性评估（人工检查 20 组测试问答），未做大规模量化评分。消融结论以"指令泄露是否消除""原文复述是否解决""检索命中率是否提升"三个可观察指标为主。

**关键发现**：

1. **指令泄露只靠 chat_template 就解决了**——不需要 Prompt 或后处理介入。
2. **原文复述需要 Prompt + 后处理双层防护**——单靠任一层都不够。
3. **检索质量对最终回答的影响最大**——查询改写 + MMR + 语义切分三者叠加后，检索命中率显著提升，回答质量随之提升。
4. **检索 > 生成 > Prompt**——在 RAG 系统中，检索环节的优化收益最大。

---

## 五、技术栈与实现细节

### 5.1 完整技术栈

| 组件 | 选型 | 说明 |
|---|---|---|
| 文档解析 | PyPDF2 / python-docx / markdown | 支持 PDF/DOCX/TXT/MD |
| 文本切分 | LangChain RecursiveCharacterTextSplitter | 语义优先切分 |
| Embedding | BAAI/bge-large-zh-v1.5 | 中文语义检索，本地运行 |
| 向量数据库 | Chroma | 持久化存储，支持 metadata |
| 检索 | LangChain Retriever + MMR | 相似度 + 多样性 |
| 生成模型 | Qwen2.5-7B-Chat（FP16 本地部署） | 中文生成质量好，7B 显存可控 |
| 聊天格式 | tokenizer.apply_chat_template | 标准化输入格式 |
| 前端 | 命令行交互（未做 Gradio 界面） | |

### 5.2 系统流程

```
用户上传文档（PDF/DOCX/TXT/MD）
    ↓
文档解析 → 提取文本
    ↓
语义切分 → 生成 chunks（带 metadata：文档名/段落位置）
    ↓
Embedding → 存入 Chroma 向量数据库
    ↓
用户提问
    ↓
查询改写（LLM 改写为更适合检索的表述）
    ↓
MMR 检索 → Top-K 相关 chunks
    ↓
chat_template 构建标准聊天格式
    ↓
强约束 System Prompt + Context + Question
    ↓
本地 LLM 生成回答
    ↓
后处理清洗（去指令泄露/检测原文复述/补来源标注）
    ↓
输出回答 + 来源溯源
```

---

## 六、教训与方法学沉淀

### 6.1 「检索质量 > 生成质量 > Prompt 工程」

这个结论和 CXMT 的"数据策略 > 模型增强 > 损失函数"同构：

| CXMT（缺陷分类） | 本项目（RAG 问答） |
|---|---|
| 数据策略（决定模型能不能学到） | 检索质量（决定模型能不能看到） |
| 模型增强（在好的数据上增量优化） | 生成质量（在好的检索上增量优化） |
| 损失函数（最后的微调手段） | Prompt 工程（最后的约束手段） |

**核心判断**：在任何"检索→生成"的级联系统中，**上游的瓶颈决定了系统的上限**。检索不到的，生成再好也是幻觉；数据不好的，模型再强也是过拟合。

### 6.2 小模型的"不可靠"需要工程化防护

小模型不像 GPT-4 那样"给个 Prompt 就靠谱"，它需要：

| 问题 | 工程化防护 |
|---|---|
| 指令泄露 | chat_template 标准化输入格式 |
| 原文复述 | 强约束 Prompt + 后处理检测 |
| 幻觉 | "不知道就说不知道"的 Prompt 约束 + 来源溯源 |
| 格式不规范 | 后处理格式校验和补全 |

> 这些工程化防护不是"技巧"，而是**让小模型在真实场景中可用的必需品**。面试时能讲清这些细节，比说"我用 LangChain 搭了个 RAG"有深度得多。

### 6.3 和前三篇文章的对比

| 维度 | CXMT | 讯飞 | 制鞋机器人 | 本项目 |
|---|---|---|---|---|
| 任务类型 | 图像分类 | 检测+定性 | 分割+3D重建 | RAG问答 |
| 输入 | 缺陷图 | X射线底片 | RGB-D鞋底 | 学术文档 |
| 核心瓶颈 | 长尾分布 | 小目标+大图 | 分割精度+点云轮廓提取+实时部署 | 检索质量+小模型 |
| 方法学 | 温和引导 | 数据策略>模型>loss | 感知-重建-轨迹递进 | 检索>生成>Prompt |
| 共同点 | 上游瓶颈决定系统上限 | 同左 | 同左 | 同左 |

---

## 七、最终成果

- 支持 PDF/DOCX/TXT/MD 多格式文档与多轮问答
- 实现 MMR 检索、多文档管理与跨文档检索、查询改写、来源溯源
- 解决小模型指令泄露与原文复述问题
- 掌握 RAG 架构设计、Prompt Engineering 与本地模型部署全链路能力
- 代码开源：https://github.com/Aeijou37/rag-academic-qa-agent

---

## 八、可复现性

本项目代码完全开源，可直接复现：

```bash
git clone https://github.com/Aeijou37/rag-academic-qa-agent.git
cd rag-academic-qa-agent
pip install -r requirements.txt
python main.py
```

---

## 九、面试预演 Q&A

**Q1：为什么不直接用 GPT-4 API，要本地部署？**
A：三个原因：①学术文档可能涉及未发表的论文/专利，上传到第三方 API 有隐私风险；②API 调用有持续费用，本地部署一次性投入后零边际成本；③我想掌握本地模型部署的全链路能力，而不是只会调 API。

**Q2：小模型和 GPT-4 在 RAG 场景下的差距主要在哪？**
A：主要在指令遵循和概括能力。GPT-4 给一个 Prompt 就能概括得很好，小模型会复制原文、泄露指令、格式不规范。我的解法是用 chat_template + 强约束 Prompt + 后处理三层工程化防护，把小模型的行为约束到可用水平。这也让我更深刻地理解了"大模型的便利是有代价的"。

**Q3：MMR 检索的原理是什么？为什么比纯相似度好？**
A：MMR 在选择下一个 chunk 时，不仅看与 query 的相似度，还看与已选 chunk 的差异度。公式是 `λ * Sim(query, doc) - (1-λ) * max(Sim(selected, doc))`。好处是避免检索到多个重复 chunk，浪费上下文窗口。在学术文档场景特别有用，因为论文的摘要和 Introduction 经常有重复内容。

**Q4：查询改写怎么做的？如果改写错了会不会更差？**
A：用 LLM 把用户问题改写为更接近文档表述的形式。比如"这个方法怎么解决长尾"→"LDAM margin"。改写确实可能出错，但 RAG 是级联系统——改写后检索不到，最多是退回到原始 query 的检索结果。我的实现是改写+原始双路检索取并集，改写路负责精准命中，原始路负责兜底召回，两路结果合并后去重再送生成。

**Q5：Chroma 为什么不用 FAISS？**
A：Chroma 自带持久化存储和 metadata 管理，对多文档场景更友好——每个 chunk 可以附带文档名、段落位置等 metadata，来源溯源直接查 metadata。FAISS 更偏底层，需要自己管理 metadata 映射。对于这个项目的规模（几十篇文档），Chroma 完全够用且开发效率更高。

**Q6：chunk_size 怎么选的？太大会怎样？太小会怎样？**
A：太大→检索精度下降（一个 chunk 里混了多个主题，Embedding 被稀释）+ 上下文窗口浪费；太小→上下文不完整（一个完整的段落被切成碎片，检索到的是半句话）。我用的是 chunk_size=800、overlap=150（中文按字符计），调优方式是人工检查检索结果——对同一组测试问题，对比不同 chunk_size 下 Top-4 检索返回的 chunk 是否覆盖了回答所需的完整上下文，选 800 是因为再大就开始混入无关段落，再小就出现半句话碎片。

**Q7：这个项目和你的工业缺陷项目有什么方法论上的关联？**
A：核心关联是"上游瓶颈决定系统上限"。CXMT 教我"数据策略 > 模型增强 > 损失函数"，RAG 项目让我验证了同样的规律在 NLP 领域成立——"检索质量 > 生成质量 > Prompt 工程"。两个项目的方法学同构，说明这是一个跨领域的通用判断。

**Q8：如果让你改进这个项目，你会做什么？**
A：三个方向：①加 Reranker——当前检索只用 Embedding 相似度，加一个 Cross-Encoder Reranker 能显著提升检索精度；②加多模态——学术文档里的图表/公式当前被丢弃了，如果能用 VLM 理解图表会大幅提升信息利用率；③加评估框架——当前评估是定性的，应该建一个学术QA测试集做量化评估。

---

## 十、总结

这个项目虽然技术栈不是最前沿的，但它让我完整走了一遍"检索→生成"系统的全链路：从文档解析、切分策略、Embedding、向量数据库、检索优化、Prompt 工程、到本地模型部署。更重要的是，它让我验证了一个跨领域的判断——**在任何级联系统中，上游瓶颈决定系统上限**。

和工业缺陷项目相比，这个项目的技术深度不在单个环节（CXMT 的 LDAM/蒸馏更深），而在**系统整合**——怎么把文档解析、检索、生成、后处理串成一条可用的链路，并针对小模型的缺陷做工程化防护。这和讯飞项目的"全流程闭环"方法学是一致的。

---

*本文为个人技术笔记。代码已开源：https://github.com/Aeijou37/rag-academic-qa-agent*
